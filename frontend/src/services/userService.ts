import { api } from "./api";

export const getUsers = async () => {
    // console.log("✅ getUsers() が呼ばれた");

    // try {
    //     const response = await api.get("/api/v1/users");
    //     console.log("✅ APIレスポンスを受信:", response.data);
    //     return response.data;
    // } catch (error) {
    //     console.error("❌ APIリクエスト失敗:", error);
    //     throw error;
    // }
};